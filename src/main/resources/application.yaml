spring:
  application:
    name: LinkBetweenUs

  datasource:
    url: jdbc:mysql://localhost:3306/Link_Between_Us?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: heyujie3405527

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: input

  data:
    redis:
      host: localhost
      port: 6379
  cache:
    type: redis


server:
  port: 8080

# JWT配置
jwt:
  secret: LinkBetweenUsSecretKeyForJWTTokenGeneration2024PleaseChangeInProduction
  expiration: 86400000  # 24小时，单位毫秒
